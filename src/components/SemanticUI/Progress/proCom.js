import React from 'react'
import { Progress } from 'semantic-ui-react'

const ProgressExampleActive = () => (
  <Progress percent={60} color='teal' progress active>
    Active
  </Progress>
)

export default ProgressExampleActive